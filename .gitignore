# Dependencies
node_modules
.pnp
.pnp.js

# Testing
coverage

# Next.js
.next
out

# Production
build
dist

# Misc
.DS_Store
*.pem

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Local env files
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE
.vscode
.idea
*.swp
*.swo
*~

# OS
Thumbs.db

# Payload CMS
payload.db
payload.db-journal
/payload-types.ts

# Local upload stub (dev image storage before Cloudinary)
/public/uploads/

# Local logs
/contact-submissions.log.json

# Hero video alt formats — keep .mp4 in repo so Vercel serves it as a
# static asset. Ignore the optional .mov / .webm variants that we'd only
# add later for codec coverage / smaller payload.
/public/hero-bg.mov
/public/hero-bg.webm
tsconfig.tsbuildinfo

# Local design-system playground HTML — not for production
public/design-system*.html

# Claude Code local config + worktrees — not for production
.claude/launch.json
.claude/settings.local.json
.claude/worktrees/
